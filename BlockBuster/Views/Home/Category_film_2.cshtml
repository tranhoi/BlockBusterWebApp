<div class="cate">
    @foreach (var items in Model)
    {
        <!-- Category Single -->
        <span class="@items.category.color.color_name"><a href="#">@items.category.name</a></span>
    }
</div>