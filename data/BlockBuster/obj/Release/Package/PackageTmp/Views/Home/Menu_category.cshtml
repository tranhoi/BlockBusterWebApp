<ul class="dropdown-menu level2">
    @foreach (var items in Model)
    {
        <li><a href="@Url.Action("film_list", "Home", new { form_id = ViewBag.form_id, cate_id = items.id, key = "none"})">@items.name</a></li>
    }
</ul>